<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Página de inicio tras el login (static/src/js/home.js) -->
    <record id="action_mgs_home" model="ir.actions.client">
        <field name="name">Inicio</field>
        <field name="tag">mgs_home</field>
        <field name="target">current</field>
    </record>

    <!-- Panel de Stock (componente OWL propio:
         static/src/js/stock_dashboard.js) -->
    <record id="action_mgs_stock_dashboard" model="ir.actions.client">
        <field name="name">Stock</field>
        <field name="tag">mgs_stock_dashboard</field>
        <field name="target">current</field>
    </record>
</odoo>
